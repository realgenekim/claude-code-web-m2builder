# M2 Bundler Gateway Credentials
# Copy this file to .env and fill in values from your operator

# Gateway URL (Cloud Run service)
GCS_GATEWAY_URL="https://gcs-upload-gateway-xxxxx.run.app"

# Authentication
GCS_GATEWAY_USER="claude"
GCS_GATEWAY_PASS="your-secret-password-here"

# Optional: Custom session ID prefix
# Defaults to: claude-web-$(date +%s)
# SESSION_ID_PREFIX="my-project"
